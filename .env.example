# tastytrade Credentials
# NEVER commit your actual .env file
#
# Option 1: OAuth (recommended)
# Create an OAuth app at https://developer.tastytrade.com
# Then either: (a) generate a refresh token via "Create Grant" in the portal,
#              (b) or complete the OAuth flow from the dashboard.
TT_CLIENT_ID=your_oauth_client_id
TT_CLIENT_SECRET=your_oauth_client_secret
TT_REFRESH_TOKEN=your_refresh_token
TT_ACCOUNT_NUMBER=your_account_number
# Required when behind a reverse proxy (must match developer portal exactly)
TT_OAUTH_REDIRECT_URI=https://your-domain.com/trader/oauth/callback
#
# Option 2: Legacy username/password (deprecated)
# TT_USERNAME=your_tastytrade_username
# TT_PASSWORD=your_tastytrade_password

# Set to 'paper' or 'live'
TRADING_MODE=paper

# Risk Management
MAX_POSITION_RISK_PCT=0.02
MAX_PORTFOLIO_RISK_PCT=0.10
MAX_POSITIONS=5
MAX_DRAWDOWN_PCT=0.15

# Logging
LOG_LEVEL=INFO
